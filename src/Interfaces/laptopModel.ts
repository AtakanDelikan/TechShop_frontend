export default interface laptopModel {
  id: number;
  name: string;
  description: string;
  price: number;
  cpu: string;
  gpu: string;
  storage: number;
  screenSize: number;
  resolution: string;
  brand: string;
  stock: number;
  image: string;
}
